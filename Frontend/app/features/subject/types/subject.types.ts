export type SubjectResponse = {
  subjectId: number;
  subjectCode: string;
  title: string;
  description: string;
};

export type SubjectListItem = {
  id: number;
  code: string;
  title: string;
  description: string;
};
